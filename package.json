{
  "name": "hot-take-app",
  "version": "1.0.0",
  "description": "A collaborative hot take prediction app built with React and Firebase.",
  "main": "src/index.js",
  "private": true,
  "dependencies": {
    "@firebase/auth": "^0.23.0",
    "@firebase/firestore": "^4.1.0",
    "@firebase/storage": "^0.11.0",
    "firebase": "^10.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "tailwindcss": "^3.3.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
